/**
 * list top user's tracks 
 * [] create a song component
 * [] iterate over the top user's tracks and render the component 
 * @returns 
 */

function SongsPreview() {
	return (
		<div>
			
		</div>
	)
}

export { SongsPreview }
